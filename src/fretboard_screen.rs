use dioxus::prelude::*;
use crate::instrument::instrument::{Instrument, FretBoard};
/*
fn display_fretboard(cx: Scope, instrument : Instrument) -> Element {
let to_render : String = "".to_string();
for (i, musicalString) in instrument.fretboard.notes.iter().enumerate(){
for(j, musicalNote) in musicalString.iter().enumerate(){
todo!();
}

}
}*/
